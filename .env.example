GOOGLE_CLOUD_PROJECT=your-project-id
STAGING_BUCKET=gs://your-bucket-name
REASONING_ENGINE_RESOURCE_NAME=projects/.../locations/.../reasoningEngines/...
CLOUD_RESOURCE_ID=//aiplatform.googleapis.com/projects/.../locations/.../reasoningEngines/...
# Optional: set to false to stop mirroring prompt/response content into Cloud Logging
# (content is still captured on spans via OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT).
OTEL_DEMO_LOG_GENAI_CONTENT=true
